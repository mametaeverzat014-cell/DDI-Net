# Exact pinned versions. Ranges were replaced with == on 2026-08-23:
# a range makes an experiment irreproducible, because a minor release of
# scikit-learn or torch can change results without any change to this repo.
#
# Full transitive closure of the environment: requirements.lock.txt
#
# NOTE: torch resolves to the CUDA build from PyPI. For a CPU-only install use
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu

# ---- Core scientific stack ---------------------------------------
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
scikit-learn==1.9.0

# ---- Cheminformatics - SMILES parsing, ECFP4, substructure highlighting 
rdkit==2026.3.5

# ---- Graphs and deep learning ------------------------------------
networkx==3.6.1
torch==2.13.0
torch_geometric==2.8.0.post1

# ---- Plumbing ----------------------------------------------------
requests==2.33.1
tqdm==4.70.0
PyYAML==6.0.1

# ---- Figures -----------------------------------------------------
matplotlib==3.11.1
seaborn==0.13.2

# ---- Tests -------------------------------------------------------
pytest==9.1.1

# ---- Not yet installed; required by the Phase A plan --------------------
# PyTDC          # Therapeutics Data Commons data loader (tdc.multi_pred.DDI)
# Pin it on first install, together with the dataset snapshot date recorded
# in DATA_PROVENANCE.md.
